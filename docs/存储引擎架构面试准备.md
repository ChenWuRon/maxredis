# 存储引擎架构 — 面试讲解文档

---

## 一、整体架构分析

### 1.1 架构全景图

```
                          ┌──────────────┐
                          │  redis-cli   │  (Jedis/Lettuce/go-redis/redis-py)
                          └──────┬───────┘
                                 │ TCP :6380
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        AcceptServer                                      │
│  server/service/dragonfly_listener.cc                                    │
│  职责：accept TCP连接 → 按 round-robin 分配到 proactor 线程              │
└─────────────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┼────────────┐
                    ▼            ▼            ▼
              ┌──────────┐ ┌──────────┐ ┌──────────┐
              │ Proactor0│ │ Proactor1│ │ Proactor2│  ... N个线程
              │ + Fiber  │ │ + Fiber  │ │ + Fiber  │
              │ + io_uring│ │+ io_uring│ │+ io_uring│
              └────┬─────┘ └────┬─────┘ └────┬─────┘
                   │            │            │
              ┌────▼────┐ ┌────▼────┐ ┌────▼────┐
              │ Shard 0 │ │ Shard 1 │ │ Shard 2 │  key hash → shard
              │ DbSlice │ │ DbSlice │ │ DbSlice │  每个 shard 绑定到一个线程
              │ hash_map│ │ hash_map│ │ hash_map│  无锁访问
              └─────────┘ └─────────┘ └─────────┘
```

### 1.2 单条请求的完整路径

```
redis-cli: SET a 1
    │
    ▼
┌─────────────────────────────────────────────────┐
│ 1. DragonflyListener                            │
│    server/service/dragonfly_listener.cc:96      │
│    new Connection(protocol_, engine_, ctx_)     │
│    创建 Connection 对象，选一个 proactor 线程    │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ 2. Connection::HandleRequests                   │
│    dragonfly_connection.cc:88                    │
│    设置 TCP_NODELAY，TLS 握手                   │
│    → InputLoop(peer)                            │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ 3. Connection::InputLoop (Fiber 协程)           │
│    dragonfly_connection.cc:175                   │
│    do-while 循环：                               │
│      DoRead()  → io_uring/epoll 读数据到 IoBuf   │
│      ParseRedis() → RESP 状态机解析              │
│      DispatchCommand() → 分发执行                │
│      ReplyReadyCommands() → 写回响应             │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ 4. Connection::ParseMultiBulk                   │
│    dragonfly_connection.cc:454                   │
│    *3\r\n$3\r\nSET\r\n$1\r\na\r\n$1\r\n1\r\n   │
│    → ParsedCommand{tokens=["SET","a","1"],argc=3}│
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ 5. Service::DispatchCommand                     │
│    main_service.cc:193                          │
│    sdstoupper("set") → "SET"                    │
│    registry_.Find("SET") → CommandId            │
│    校验 arity (-3: 最少3个参数)                  │
│    → cid->Invoke(args, cntx)                    │
└─────────────────────────────────────────────────┘
    │
    ├─────────────┬──────────────────┐
    │ 读命令 GET   │ 写命令 SET/DEL   │ PING等特殊命令
    ▼              ▼                  ▼
┌──────────────┐ ┌──────────────────┐ ┌─────────────┐
│ 直接访问      │ │ RaftEngine       │ │ kv_.Apply   │
│ DbSlice::Find │ │ SubmitCommand    │ │ (直接执行)  │
│ (无Raft)      │ │ →FastCommitPath  │ │             │
└──────────────┘ └──────────────────┘ └─────────────┘
    │              │
    ▼              ▼
┌──────────────────────────────────────────────────┐
│ 6. KvStateMachine / DbSlice                      │
│    Shard(key) → Await(sid, lambda)               │
│    → es->db_slice.AddOrFind / Find               │
│    → absl::flat_hash_map 操作                     │
└──────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ 7. ReplyBuilder / RespSerializer                │
│    reply_builder.cc                             │
│    SET → "+OK\r\n"                              │
│    GET → "$1\r\n1\r\n"                          │
│    ERR → "-ERR msg\r\n"                         │
│    writev() 零拷贝写回 socket                    │
└─────────────────────────────────────────────────┘
```

### 1.3 各层职责速查

| 层级 | 文件 | 职责 |
|------|------|------|
| AcceptServer | `helio/util/accept_server.h` | SO_REUSEPORT，多个线程独立 accept |
| DragonflyListener | `service/dragonfly_listener.cc` | 每个新 TCP 连接创建 Connection |
| Connection | `service/dragonfly_connection.cc` | Fiber I/O 循环，RESP 解析，分发，回复 |
| RedisParser | `protocol/redis_parser.cc` + `connection.cc:454` | RESP 状态机解析，半包处理 |
| CommandRegistry | `service/command_registry.cc` | O(1) hash 查找，命令分发 |
| MainService | `service/main_service.cc` | 业务入口，读写路径分流 |
| RaftEngine | `raft/raft_engine.cc` | 写命令 Raft 共识，单节点 FastCommit |
| KvStateMachine | `state_machine/kv_state_machine.cc` | 命令解析 + 跨线程投递到分片 |
| EngineShardSet | `storage/engine_shard_set.cc` | 分片管理，跨线程 Await/Schedule |
| DbSlice | `storage/db_slice.cc` | 每个分片的 KV 存储，flat_hash_map |
| ReplyBuilder | `protocol/reply_builder.cc` | RESP 响应编码，writev 发送 |

### 1.4 为什么这样设计

1. **分片 + 线程绑定**：每个 CPU 核心处理自己的 Shard，数据无锁访问，零竞争
2. **Fiber 协程**：每个连接一个 fiber，轻量（64KB 栈），数万连接无压力
3. **io_uring**：批量提交 I/O 请求，一次系统调用完成的 I/O 操作比 epoll 多
4. **读写分离**：读不经过 Raft，延迟更低
5. **单节点 FastCommitPath**：没有 peer 时 Append→Commit→Apply 三步同步完成，无网络开销

---

## 二、Sharded Storage

### 2.1 为什么要分片

**核心问题**：单线程 Redis 只能用一个 CPU 核心，QPS 受限于单核能力。

**分片解决**：数据按 key 分到 N 个 CPU 核心，每个核心独立处理自己的分片。N 个核心 → 理论 N 倍吞吐。

```
不分片：                   分片 (N=4)：
  ┌─────────┐               Thread 0    Thread 1    Thread 2    Thread 3
  │ 所有key  │               keys A-F    keys G-M    keys N-T    keys U-Z
  │  单核处理 │               ───────     ───────     ───────     ───────
  └─────────┘               无锁访问     无锁访问     无锁访问     无锁访问
  1x 吞吐                   4x 吞吐
```

### 2.2 Key 如何路由到 Shard

```cpp
// storage/common_types.h:47-50
template <typename View>
inline ShardId Shard(const View& v, ShardId shard_num) {
  XXH64_hash_t hash = XXH64(v.data(), v.size(), 120577);
  return hash % shard_num;
}
```

**流程**：
1. 用 `XXH64`（极快的非加密 hash）计算 key 的 hash 值
2. `hash % shard_num` 得到分片 ID
3. 后续所有对该 key 的操作都投递到该分片对应的线程

### 2.3 为什么相同 Key 进入同一个 Shard

因为在 hash 函数不变的情况下，`XXH64("a") % N` 恒等于同一个值。

这保证了：
- 同一个 key 的所有操作（SET → GET → DEL）都在同一个分片线程中执行
- **串行化**：对该 key 的操作天然串行，无需分布式锁
- **一致性**：Raft 的 `ApplyLogEntry` 在一条日志被 commit 后，按 index 顺序执行，到达分片线程后按 `FiberQueue` 的 FIFO 顺序处理

### 2.4 为什么不同 Shard 可以并行

- 线程 0 处理 Shard 0 的 key（如 "aaa"）
- 线程 1 处理 Shard 1 的 key（如 "bbb"）

两个操作访问不同的 `flat_hash_map`（在不同线程的内存空间中），完全无交集。“aaa”的 SET 和“bbb”的 GET 可以同时在两个不同 CPU 核心上执行。

### 2.5 为什么几乎不用加锁

```cpp
// engine_shard_set.h:31-33
class EngineShard {
  DbSlice db_slice;                          // 只被本线程访问
  static thread_local EngineShard* shard_;    // thread_local 单例
};
```

关键设计：
1. 每个 `EngineShard` 是 `thread_local` 的，只在本线程创建和访问
2. `DbSlice` 内部的 `flat_hash_map` 只有本线程能访问，不需要 mutex
3. 跨线程访问通过 **消息传递**：`shard_set_->Await(sid, lambda)` 把任务投递到目标线程的 `FiberQueue`，目标线程执行完再返回

```cpp
// kv_state_machine.cc:53-57
void KvStateMachine::Set(DbIndex db_ind, string_view key, string_view val) {
  ShardId sid = Shard(key);  // 计算目标分片
  shard_set_->Await(sid, [db_ind, key, val] {
    EngineShard* es = EngineShard::tlocal();   // 在目标线程执行
    auto [it, res] = es->db_slice.AddOrFind(db_ind, key);
    it->second.value = val;
  });
}
```

**这里的关键**：`Await` 将 lambda 序列化到目标线程执行，调用者 fiber 挂起等待结果。数据本身不跨线程共享，只有 lambda 闭包传递。

### 2.6 EngineShardSet 和 DbSlice 源码解析

```cpp
// engine_shard_set.h:59-96
class EngineShardSet {
  util::ProactorPool* pp_;
  vector<FiberQueue*> shard_queue_;  // 每个分片一个 FiberQueue

  // 跨线程投递 + 同步等待结果
  template <typename F> auto Await(ShardId sid, F&& f) {
    return shard_queue_[sid]->Await(forward<F>(f));
  }

  // 跨线程投递，不等待
  template <typename F> auto Add(ShardId sid, F&& f) {
    return shard_queue_[sid]->Add(forward<F>(f));
  }

  // 所有分片并行执行
  template <typename U> void RunBriefInParallel(U&& func) {
    BlockingCounter bc{size()};
    for (uint32_t i = 0; i < size(); ++i) {
      pp_->at(i)->DispatchBrief([f = func, bc]() mutable {
        f(EngineShard::tlocal());
        bc->Dec();
      });
    }
    bc->Wait();  // 等待所有分片完成
  }
};
```

```cpp
// engine_shard_set.cc:28-33
EngineShard::EngineShard(ShardId index)
    : db_slice(index, this), queue_(kQueueLen) {
  fiber_q_ = fb2::Fiber("shard_queue" + index, [this] {
    queue_.Run();  // 启动 FiberQueue 的处理 fiber
  });
}
```

每个 `EngineShard` 内部有一个 `FiberQueue`（MPSC 队列）和对应的处理 fiber。外部线程 `Await(sid, task)` 时，task 被 push 到队列，处理 fiber 取出执行，把结果传回调用者。

```cpp
// db_slice.cc:42-64
auto DbSlice::Find(DbIndex db_index, string_view key) -> OpResult<MainIterator> {
  auto& db = db_arr_[db_index];
  MainIterator it = db.main_table->find(key);           // flat_hash_map::find
  if (it == db.main_table->end()) {
    return OpStatus::KEY_NOTFOUND;
  }
  // 检查过期
  if (it->second.expire_ms != 0 && it->second.expire_ms <= NowMs()) {
    db.stats.obj_memory_usage -= (it->first.capacity() + it->second.value.capacity());
    db.main_table->erase(it);
    return OpStatus::KEY_NOTFOUND;
  }
  return it;
}
```

**面试回答模板**：

> 我们采用 key-hash 分片架构，用 XXH64 对 key 做 hash 然后模分片数确定目标分片。每个分片绑定到一个 proactor 线程，拥有独立的 `flat_hash_map`，访问完全无锁。跨线程通过 `FiberQueue` 消息传递，`Await(sid, lambda)` 把任务投递到目标线程，调用者 fiber 挂起等待结果。同一 key 的所有操作都在同一分片串行执行，天然保证一致性。

---

## 三、Fiber 调度

### 3.1 Fiber 原理

Fiber 是**用户态协程**，基于 `boost.context` 的 `jump_fcontext` 实现栈切换。

```cpp
// helio/util/fibers/fibers.h:15-70
class Fiber {
  // 默认栈大小 64KB（线程栈是 8MB）
  struct Opts {
    Launch launch = Launch::post;
    FiberPriority priority = FiberPriority::NORMAL;
    string name;
    uint32_t stack_size = 64 * 1024;  // ← 轻量！
  };
  
  template <typename Fn>
  Fiber(string_view name, Fn&& fn)  // 创建一个 fiber 执行 fn
    : impl_{MakeWorkerFiberImpl(name, fn)} { Start(Launch::post); }
};
```

### 3.2 Fiber vs 线程 vs 系统协程

| | Thread | 系统协程 (goroutine) | Fiber (boost.context) |
|------|------|------|------|
| 栈大小 | 8MB (固定) | 2KB 起步，可增长 | 64KB (固定) |
| 切换开销 | ~1-2us (内核态) | ~200ns | ~50ns (纯用户态) |
| 调度者 | OS 内核 | Go Runtime | 项目自己的 Scheduler |
| 并发数 | 数千 | 数十万 | 数万 |
| 切换时机 | 抢占式 | 抢占+协作 | 协作式 (SleepFor/Yield) |
| 内存开销 | 大 | 小 | 极小 |

**为什么 Fiber 切换快**：
- 纯用户态，不陷入内核
- 只保存/恢复 callee-saved 寄存器 + 栈指针（~16 个寄存器）
- `jump_fcontext` 是手写汇编，极快

### 3.3 项目中的 Fiber 实例

#### 3.3.1 InputLoop Fiber（每个连接一个）

```cpp
// dragonfly_connection.cc:175
void Connection::InputLoop(FiberSocketBase* peer) {
  do {
    DoRead(peer, RecvNotification{}, &io_buf);    // io_uring/epoll 异步读
    evc_.await([&] {                              // fiber 挂起等待数据
      return io_buf.InputLen() > min_parse_threshold || ec_;
    });
    ParseRedis(&io_buf);                          // 解析 RESP
    service_->DispatchCommand(...);                // 分发执行
    cc_->ReplyReadyCommands();                     // 写回响应
  } while (peer->IsOpen());
}
```

#### 3.3.2 Heartbeat Fiber（Leader 专用）

```cpp
// raft_node.cc:373-409
void RaftNode::HeartbeatLoop() {
  while (!shutdown_ && role_ == RaftRole::Leader) {
    SendHeartbeatToPeers();        // 向所有 peer 发心跳
    ExtendLeaderLease();           // 刷新 leader 租约
    // leader transfer 超时检查...
    ThisFiber::SleepFor(50ms);     // fiber 睡眠 50ms（不阻塞线程！）
  }
}
```

**启动**：`BecomeLeader()` 时 `heartbeat_fiber_ = Fiber("heartbeat", [this] { HeartbeatLoop(); })`
**停止**：角色变为 Follower/Candidate 时调用 `StopHeartbeat()`

#### 3.3.3 Election Fiber（Follower 专用）

```cpp
// election_timer.cc
void ElectionTimer::Run() {
  while (!shutdown_) {
    int timeout_ms = dist_(rng_);  // 随机 150~300ms（Raft 论文要求）
    ThisFiber::SleepFor(milliseconds(timeout_ms));
    // 如果 SleepFor 期间被 Reset() 过，epoch 会变化，跳过
    if (epoch_.load() != saved) continue;
    active_.store(false);
    if (cb_) cb_();  // 触发 OnElectionTimeout → BecomeCandidate
  }
}
```

**关键设计**：
- `Reset()` 递增 `epoch_` 计数器——如果在 sleep 期间收到了合法心跳，唤醒后检测到 epoch 变化，重新开始计时
- `Deactivate()` 暂停计时但不停止 fiber，避免频繁创建销毁
- `dist_{150, 300}` 随机间隔避免多个 follower 同时发起选举（split vote）

#### 3.3.4 Snapshot Fiber

```cpp
// snapshot_fiber.cc
void SnapshotFiber::Run() {
  while (!shutdown_) {
    uint64_t now = NowMs();
    bool time_trigger = (now - last_snapshot_time_ms_) >= time_threshold_sec_ * 1000;
    bool cmd_trigger = write_count_ >= cmd_threshold_;
    if (time_trigger || cmd_trigger) {
      service_->CreateSnapshot();  // 触发快照
      last_snapshot_time_ms_ = now;
      write_count_ = 0;
    }
    ThisFiber::SleepFor(1s);  // 每秒检查一次
  }
}
```

`NotifyWrite()` 被 `Service::Set/Del/Expire` 调用，递增原子计数器 `write_count_`。

### 3.4 EngineShard::Await() 实现原理

```cpp
// engine_shard_set.h:55-58
template <typename F>
auto EngineShardSet::Await(ShardId sid, F&& f) {
  return shard_queue_[sid]->Await(std::forward<F>(f));
}
```

`shard_queue_[sid]` 是目标分片的 `FiberQueue`。`Await` 将 lambda 推入队列：
1. 目标线程的 `FiberQueue::Run()` fiber 从队列取出 lambda 执行
2. 执行完成后，通过 `Done`/`EventCount` 唤醒调用方 fiber
3. 调用方 fiber 拿到返回值，继续执行

```
调用者线程 (Thread 0)              目标线程 (Thread 3, Shard 3)
     │                                     │
     │ Await(sid=3, lambda) ─────────────► │
     │ (fiber挂起)                          │ FiberQueue::Run()
     │                                     │ 取出lambda执行
     │                                     │ lambda(begin,end)
     │                                     │ Done::Notify()
     │ ◄──── 唤醒 ─────────────────────── │
     │ 拿到返回值                            │
     ▼                                     ▼
```

### 3.5 为什么 Fiber 适合 KV 系统

1. **高并发连接**：每个连接一个 fiber（64KB），内存开销远小于每连接一线程（8MB）
2. **I/O 密集**：Fiber 在 I/O 等待时 `SleepFor`/`await` 不阻塞线程，线程可以处理其他 fiber
3. **协作式调度**：没有抢占开销，适合短任务（SET/GET 都是微秒级）
4. **无锁同步**：`EventCount`、`Done`、`Mutex` 都是 fiber 友好的，阻塞只让出 CPU 不阻塞线程
5. **定时任务**：Heartbeat（50ms）、Election（150-300ms）、Snapshot（1s）都是 fiber `SleepFor`，不占线程资源

**面试回答模板**：

> 我们基于 boost.context 实现了用户态 Fiber，默认栈只有 64KB，切换成本 ~50ns，纯用户态不进入内核。每个 TCP 连接一个 Fiber 处理 InputLoop，通过 `evc_.await` 挂起等待 I/O 事件。Raft 的选举定时器和心跳都是独立 Fiber，用 `SleepFor` 实现非阻塞等待。跨分片通过 `FiberQueue::Await` 投递任务，目标线程执行完成后用 EventCount 唤醒调用方 Fiber。这套机制的核心优势是：数万连接共享少量线程，I/O 等待不浪费 CPU，协作式调度没有抢占开销。

---

## 四、io_uring

### 4.1 Linux 传统 I/O 模型

```
应用程序                    内核
  write(fd, buf, len)  ───►  拷贝数据到内核缓冲区
    (阻塞/返回)        ◄───   内核调度磁盘/网络
                       
  问题：
  - 每次 I/O 都是一次系统调用 (syscall)，用户态↔内核态切换有开销
  - epoll 需要两次系统调用：epoll_wait() + read()，each event = 2 syscalls
```

### 4.2 epoll 的问题

```
epoll 模型：
  epoll_wait(efd, events, MAXEVENTS, timeout)  ← syscall 1
  for each event:
    read(fd, buf, len)                          ← syscall 2 (per event)
    process(buf)

1万个事件 = 10001 次系统调用
```

### 4.3 io_uring 优势

```
io_uring 模型：
  ┌──────────────────────┐
  │   Submission Queue   │  (SQ)  用户→内核，共享内存，无 syscall 写入
  │   [sqe1][sqe2]...   │
  └──────────────────────┘
            │
    io_uring_enter()  ← 批量提交（仅在需要时调用一次 syscall）
            │
  ┌──────────────────────┐
  │  Completion Queue    │  (CQ)  内核→用户，共享内存
  │  [cqe1][cqe2]...    │
  └──────────────────────┘

1万个事件 = 可能只需 1 次 io_uring_enter() + 1 次大的批量处理
```

**核心优势**：
1. **批量提交**：多个 SQEs 一次 `io_uring_enter()` 提交给内核
2. **批量收割**：一次 `io_uring_wait_cqes()` 收割多个完成的 CQEs
3. **零拷贝**：共享内存做 SQ/CQ 环形缓冲区，无数据拷贝
4. **Fixed buffers/files**：预注册内存和文件描述符，减少每次操作的内核处理
5. **SQ poll**：内核主动轮询 SQ，不需要系统调用

### 4.4 SQ/CQ 工作流程

```
用户程序:
  1. io_uring_get_sqe(ring) → 获取空闲 SQE
  2. io_uring_prep_readv(sqe, fd, iovecs, nr_vecs, offset)  ← 填入操作
  3. io_uring_submit(ring) → 提交所有 SQE，可能无 syscall
  ... 处理其他事情 ...
  4. io_uring_wait_cqes(ring, &cqes, count, ...) → 收割完成的 CQE
  5. 处理每个 cqe->res (返回值)
  6. io_uring_cqe_seen(ring, cqe) → 标记已消费
```

### 4.5 为什么减少 syscall 和 context switch

| | epoll | io_uring |
|------|------|------|
| 每个 accept | 1 syscall | 可能 0（SQ poll） |
| 每个 read | 1 syscall | 可能 0（SQ poll 或 batch） |
| 每个 write | 1 syscall | 可能 0 |
| 上下文切换 | 每次 syscall 都切换 | 批量处理，次数少几十倍 |
| 内存拷贝 | read 从内核拷贝到用户缓冲区 | Fixed buffers 省去一次拷贝 |

### 4.6 项目中使用 io_uring 的地方

**启动时**（`dfly_main.cc:118`）：
```cpp
pp.reset(fb2::Pool::IOUring(1024, 0));  // 创建 io_uring 线程池，ring 大小 1024
```

**Connection 的读取**（`dragonfly_connection.cc:179-190`）：
```cpp
#ifdef __linux__
if (socket_->proactor()->GetKind() == ProactorBase::IOURING) {
  auto* up = static_cast<fb2::UringProactor*>(socket_->proactor());
  bool enable_multishot = up->BufRingEntrySize(kRecvSockGid) > 0;
  if (enable_multishot) {
    fb2::UringSocket* usock = static_cast<fb2::UringSocket*>(peer);
    usock->set_bufring_id(kRecvSockGid);
    usock->EnableRecvMultishot();  // 多 shot recv：一个 SQE 持续接收
  }
}
```

**BufRing**（`dfly_main.cc:41-68`）：
```cpp
void RegisterBufRings(ProactorPool* pool) {
  auto bufcnt = GetFlag(FLAGS_uring_recv_buffer_cnt);
  pool->AwaitBrief([&](unsigned, ProactorBase* pb) {
    auto up = static_cast<fb2::UringProactor*>(pb);
    up->RegisterBufferRing(kRecvSockGid, bufcnt, kRecvBufSize);
  });
  // kRecvBufSize = 256 字节，每个线程预分配 bufcnt 个缓冲区
  // 这样 recv 的数据直接写入预注册的缓冲区，不用每次 malloc
}
```

**INFO 命令报告**（`main_service.cc:457`）：
```cpp
absl::StrAppend(&info, "multiplexing_api:iouring\r\n");
```

**面试回答模板**：

> 我们底层 I/O 用 io_uring 替代了传统的 epoll。核心收益是批量提交和批量收割——一次 `io_uring_enter` 可以提交多个读/写请求，一次 `io_uring_wait_cqe` 可以收割多个完成事件。同时利用 Buffer Ring 预注册 256 字节的接收缓冲区，recv 数据直接写入预分配内存，避免每次 malloc。Multishot Recv 模式下一个 SQE 可以持续接收数据直到缓冲区满，进一步降低提交频率。相比 epoll 模型，系统调用次数降低一到两个数量级。

---

## 五、存储引擎

### 5.1 DbSlice 实现

```cpp
// db_slice.h:21-81
class DbSlice {
  ShardId shard_id_;
  EngineShard* owner_;
  
  struct DbRedis {
    unique_ptr<MainTable> main_table;  // absl::flat_hash_map<string, MainValue>
    InternalDbStats stats;
  };
  vector<DbRedis> db_arr_;  // 支持多数据库 (db0, db1...)
};
```

```cpp
// common_types.h:56-63
struct PrimeValue {
  string value;
  uint64_t expire_ms = 0;  // 过期时间戳（毫秒），0 = 永不过期
};
using MainValue = PrimeValue;
using MainTable = absl::flat_hash_map<string, MainValue>;
```

**关键操作**：

| 操作 | 方法 | 复杂度 | 源码位置 |
|------|------|------|------|
| 查找 | `Find(db, key)` | O(1) 平均 | db_slice.cc:42 |
| 查找或创建 | `AddOrFind(db, key)` | O(1) 平均 | db_slice.cc:78 |
| 删除 | `Del(db, key)` | O(1) 平均 | db_slice.cc:107 |
| 设置过期 | `SetExpire(db, key, ms)` | O(1) | db_slice.cc:117 |
| 详情 | `DbSize(db)` | O(1) 返回 size() | db_slice.cc:158 |
| 遍历 | `Traverse(db, visitor)` | O(N) | db_slice.cc:148 |

**过期处理**：懒惰删除——`Find` 和 `AddOrFind` 时检查 `expire_ms <= NowMs()`，如果过期则 `erase` 并返回 `KEY_NOTFOUND`。

### 5.2 为什么 absl::flat_hash_map 比 std::unordered_map 更好

| | `std::unordered_map` | `absl::flat_hash_map` |
|------|------|------|
| 底层结构 | 分离链地址法（链表桶） | 开放寻址（open addressing） |
| 内存布局 | 节点分散在堆上 | 连续数组存储 |
| Cache Miss | 严重（追踪指针） | 少（线性探测） |
| 插入开销 | malloc + 链表操作 | 数组插入（可能触发 rehash） |
| 查找开销 | 链表遍历 | 线性探测 + SIMD 优化 |
| 迭代速度 | 慢（链表遍历） | 快（数组遍历） |
| 删除 | O(1) 链表操作 | Tombstone 标记 |

### 5.3 Swiss Table 原理（absl::flat_hash_map 的实现）

Swiss Table 是 Google 在 Abseil 中实现的 hash 表，核心设计：

1. **元数据与控制分离**：用一个单独的 `ctrl_` 数组存储每个槽位的状态（EMPTY/DELETED/低7位 hash），用 SIMD 一次比较 16 个槽位的 hash
2. **SSE2/NEON 加速探测**：`_mm_cmpeq_epi8` 一次比较 16 个字节，快速找到候选槽位
3. **连续内存**：所有条目存储在连续的堆数组上，对 CPU cache 友好
4. **负载因子**：默认 `7/8 = 87.5%`（vs std::unordered_map 的 ~70%）

```
Swiss Table 内存布局:

  ctrl_[0..15]   slots[0..15]
  ┌──────────┐   ┌──────────────┐
  │H|E|D|H|.. │   │k1|v1|k2|v2|..│  ← 连续内存
  └──────────┘   └──────────────┘
  SSE2一次比较16个ctrl

查找流程:
  1. hash(key) → H1(高位), H2(低7位)
  2. SSE2对比 ctrl_ 中所有等于 H2 的位置
  3. 对每个候选位置，比较完整 key
```

### 5.4 Cache Locality

传统 `std::unordered_map`（分离链地址法）：

```
bucket[0] → node1 → node2 → node3
  │          (heap)  (heap)  (heap)
  │          ↑ cache miss  ↑ cache miss
```

Swiss Table（开放寻址）：

```
slots[0..N]:  ←──── 连续内存 ────→
  [k,v][k,v][k,v][k,v][k,v]...[k,v]
  
CPU 一次加载一个 cache line (64B) 可以包含 3-4 个 entry
```

对于 KV 系统来说：
- **GET 操作**就是一次 `flat_hash_map::find(key)`，大部分情况在 1-2 次探测内命中
- **SET 操作**是一次 `emplace`，可能触发 rehash（扩容），但 Swiss Table 的 load factor 高，rehash 频率低
- 连续迭代（`Traverse`）是顺序扫描数组，对 CPU prefetch 友好

### 5.5 为什么 GET 性能更高

1. **读不经过 Raft**：直接 `Schedule` 到分片线程 → `DbSlice::Find`
2. **无锁**：分片线程单线程访问 `flat_hash_map`
3. **hash 表查找极快**：Swiss Table 的 SIMD 加速探测
4. **无磁盘 I/O**：纯内存操作
5. **零拷贝**：`Find` 返回 `MainIterator`，value 直接引用 hash 表中的 `string`

### 5.6 数据写入流程（SET 为例）

```
Client: SET a 1
  → ParseMultiBulk: tokens=["SET","a","1"]
  → DispatchCommand → registry_["SET"] → Service::Set
  → RaftEngine::SubmitCommand(cid, args)
  → CommandEncoder::Encode → ReplicatedCommand{SET, ["SET","a","1"]}
  → 单节点: FastCommitPath
    → LogEntry → Append → AdvanceCommitIndex(0→1) → ApplyCommittedLogs
    → KvStateMachine::ApplyLogEntry("SET a 1")
    → Shard("a") → shard_set_->Await(sid=hash("a")%N, lambda)
    → EngineShard::tlocal()->db_slice.AddOrFind(0, "a")
      → db.main_table->emplace("a", MainValue{})    ← 插入hash表
      → it->second.value = "1"                      ← 设置值
  → ReplyBuilder::SendStored() → "+OK\r\n"
```

**面试回答模板**：

> 存储引擎的核心是 per-shard 的 `absl::flat_hash_map`。选择 Abseil 的 Swiss Table 而不是 `std::unordered_map`，是因为开放寻址 + 连续内存布局对 CPU cache 更友好，SSE2 SIMD 一次比较 16 个槽位的 hash。过期处理用懒惰删除——Find 时检查 expire_ms，过期直接 erase。写入走 Raft 共识路径，Apply 阶段通过 `Await(sid)` 跨线程投递到目标分片，在目标线程内无锁操作 hash 表。

---

## 六、完整请求流程（SET 和 GET 对比）

### 6.1 SET a 1 完整流程

```
Step 1: TCP 连接
  redis-cli 连接 :6380
  → AcceptServer 接受连接
  → DragonflyListener::NewConnection(proactor)
  → new Connection(Protocol::REDIS, service, ctx)
  → Connection::HandleRequests → InputLoop(peer)

Step 2: 读取数据 (io_uring/epoll)
  DoRead → TryRecv(buf) → io_buf.CommitWrite(bytes)
  → evc_.notify() 唤醒 InputLoop fiber

Step 3: RESP 解析
  state_==INIT, input[0]=='*' → state_=PARSE_MULTIBULK
  → ParseMultiBulk(io_buf):
    *3\r\n      → multibulk_len_ = 3, 预分配 tokens[3]
    $3\r\nSET   → tokens[0] = sdsnewlen("SET", 3), multibulk_len_=2
    $1\r\na     → tokens[1] = sdsnewlen("a", 1),    multibulk_len_=1
    $1\r\n1     → tokens[2] = sdsnewlen("1", 1),    multibulk_len_=0
    → parse_complete = 1, argc = 3

Step 4: 命令分发
  Service::DispatchCommand:
    sdstoupper(tokens[0]) → "SET"
    registry_.Find("SET") → CommandId{name="SET", arity=-3, mask=WRITE|DENYOOM}
    校验 argc >= 3 ✓
    → cid->Invoke(args, cntx) → Service::Set(args, cntx)

Step 5: Raft 共识 (写路径)
  Service::Set:
    CmdArgVec cmd_vec → engine_.SubmitCommand(cid, args)
    → RaftEngine::SubmitCommand:
      CommandEncoder::Encode → ReplicatedCommand{SET, ["SET","a","1"]}
      role() != Leader → 报错 (只Leader能写)
      PeerCount() == 0 → FastCommitPath:
        ① group_.log_storage()->Append(entry)    // term=1, cmd="SET a 1"
        ② group_.node().AdvanceCommitIndex()      // commit_index: 0→1
        ③ group_.node().ApplyCommittedLogs()
          → last_applied: 0→1
          → KvStateMachine::ApplyLogEntry("SET a 1")
            → 解析 "SET" "a" "1"
            → Set(0, "a", "1")
              → Shard("a") = XXH64("a",120577) % shard_num
              → shard_set_->Await(sid, [&]{
                  EngineShard::tlocal()->db_slice.AddOrFind(0, "a")
                  → db.main_table->emplace("a", MainValue{})
                  → it->second.value = "1"
                })

Step 6: 响应编码
  result.op == OK → cntx->SendStored()
  → ReplyBuilder::SendOk() → RespSerializer::SendSimpleString("OK")
  → "+OK\r\n" → writev(socket, {"+OK\r\n"}) → 发送到 socket

Step 7: 持久化 (AOF)
  persistence_manager_->RecordCommand(["SET","a","1"])
  snapshot_fiber_.NotifyWrite()  // 快照计数 +1
```

### 6.2 GET a 完整流程

```
Step 1-3: 同上 (TCP连接 → 读取 → RESP解析)
  → tokens=["GET","a"], argc=2

Step 4: 命令分发
  registry_.Find("GET") → CommandId{name="GET", arity=2, mask=READONLY|FAST}
  → Service::Get(args, cntx)

Step 5: 直接读取 (读路径，绕过 Raft)
  Service::Get:
    key = "a"
    auto cb = [cntx](EngineShard* es) {
      OpResult<MainIterator> res = es->db_slice.Find(0, "a")
      // ↑ 直接查本地 hash 表，O(1)
      if (res) {
        cntx->SendGetReply(key, 0, res->value.second.value, cmd)
        // → "$1\r\n1\r\n" 编码响应
      } else {
        cntx->SendGetNotFound(cmd)
        // → "$-1\r\n" (nil)
      }
    }
    engine_.Schedule(0, key, cb)
    //    ↑ Schedule = 投递到分片线程，不经过 Raft

Step 6: 响应编码
  SendGetReply → SendBulkString("1") → "$1\r\n1\r\n" → writev → socket
```

### 6.3 读写路径对比

| | GET (读) | SET (写) |
|------|------|------|
| Raft 参与 | 不参与 | FastCommitPath (单节点) / ReplicateLog (多节点) |
| 日志追加 | 无 | Append entry |
| commit_index | 不变 | +1 |
| 跨线程 | Schedule → 目标分片 | Await → 目标分片 |
| 锁 | 无锁 | 无锁 |
| 延迟 | ~1次 hash 查找 | Append + Commit + Apply |
| AOF | 不写 | RecordCommand |

---

## 七、性能优化

### 7.1 Sharding — 水平扩展

```
不分片 (单线程):              分片 (N=4):
  1 CPU 核心, 1 hash_map      4 CPU 核心, 4 hash_map
  QPS = 1x                     QPS = 理论 4x

收益：
  - 不同 key 的操作在不同核心并行处理
  - 每个核心独立的 L1/L2 cache，cache 命中率更高
  - 无锁竞争，无 false sharing
```

### 7.2 Fiber — 高并发低成本

```
Thread 模型:                     Fiber 模型:
  1万个连接 = 1万个线程            1万个连接 = 1万个 fiber (4个线程)
  内存: 10000 × 8MB = 80GB       内存: 10000 × 64KB = 640MB
  上下文切换: 内核调度, ~1us        上下文切换: 用户态 jmp, ~50ns
  I/O 模型: 线程阻塞               I/O 模型: fiber 挂起，线程继续工作
```

### 7.3 io_uring — 减少系统调用

```
epoll:                            io_uring:
  每个 I/O: syscall × 2            每个 batch: syscall × 1 (或 0)
  1万事件 ≈ 20001 syscalls        1万事件 ≈ 1-3 syscalls
  每次 syscall: ~100ns overhead   批量处理, overhead 分摊
```

### 7.4 absl::flat_hash_map — Cache 友好

```
std::unordered_map:                 absl::flat_hash_map:
  bucket → node → node (链表)       连续数组 [k,v][k,v]...
  每次查找: 追指针, cache miss 多    每次查找: 连续探测, cache hit 高
  SSE 加速: 无                      SSE2: 一次比较 16 个槽
  Load factor: ~70%                 Load factor: 87.5% (= 更省内存)
```

### 7.5 零拷贝

| 阶段 | 传统做法 | 项目做法 |
|------|------|------|
| RESP 解析 | 拷贝到新 buffer | `bulk_str = str.subspan(0, len)` (引用 IoBuf) |
| 参数传递 | 拷贝到 `vector<string>` | `CmdArgList = Span<MutableStrSpan>` (引用原数据) |
| 响应发送 | `sprintf` + `write` | `writev` gather-scatter 多段发送 |
| 跨线程投递 | 拷贝参数 | lambda 闭包捕获，移动语义 |

### 7.6 读写分离

不考虑 Raft 去处理读请求：
- 读请求不产生 Raft 日志
- 读请求不等待多数派
- `commit_index` 不变化
- P99 延迟更低（不需要等复制）

**面试回答模板**：

> 性能优化主要体现在四个维度：① 分片水平扩展——key hash 到多个 CPU 核心并行处理，无锁；② Fiber 轻量并发——64KB 栈，数万连接共享少量线程；③ io_uring 批量 I/O——系统调用数降低 1-2 个数量级，零拷贝 buffer ring；④ Swiss Table 存储——开放寻址 + SIMD 加速探测，连续内存对 CPU cache 友好。单机环境下 GET 延迟在微秒级，SET 单节点走 FastCommitPath，只有 Append+Commit+Apply 三步，没有网络开销。

---

## 八、测试体系

### 8.1 测试全景

项目共 **32 个测试文件**，覆盖 7 大类：

| 类别 | 测试数 | 代表文件 |
|------|------|------|
| Raft 核心 | 5 | `raft_node_test`, `raft_role_test`, `raft_engine_test`, `election_timer_test`, `raft_group_test` |
| Raft RPC | 5 | `vote_rpc_test`, `append_entries_rpc_test`, `heartbeat_rpc_test`, `install_snapshot_rpc_test`, `raft_types_test` |
| Raft 日志 | 4 | `command_log_test`, `log_storage_test`, `file_log_storage_test`, `segment_log_storage_test` |
| Raft Snapshot | 6 | `snapshot_meta_test`, `snapshot_writer_test`, `snapshot_loader_test`, `snapshot_sender_test`, `snapshot_receiver_test`, `raft_snapshot_manager_test` |
| Raft 集成 | 3 | `raft_integration_test`, `raft_apply_recovery_test`, `raft_multi_group_test` |
| 状态机/持久化 | 5 | `kv_state_machine_test`, `state_serializer_test`, `snapshot_manager_test`, `aof_writer_test`, `command_serializer_test` |
| 协议解析 | 2 | `redis_parser_test`, `memcache_parser_test` |

### 8.2 Raft 核心测试

**raft_node_test.cc** — Raft 节点状态机：

```
测试点：
  - 初始状态：Follower, term=0, voted_for="", vote_count=0
  - BecomeFollower(term): 更新 term, 重置 voted_for
  - BecomeCandidate: term++, 自投票, vote_count=1
  - BecomeLeader: 变为 Leader, term 不变
  - OnRequestVote: 按 Raft 规则投票
    规则：请求term > 自己的term → 投票
         请求term == 自己的term && voted_for是空或请求者 → 投票
         请求term < 自己的term → 拒绝
    规则：请求的log至少和自己一样新 → 投票
  - OnHeartbeat: term >= 自己的term → 变为Follower
  - OnAppendEntries: 日志复制，commit_index 推进
  - SetRole 统一入口：管理 timer 和 heartbeat fiber 生命周期
```

**raft_role_test.cc** — 角色转换（时间模拟 + 模拟 RPC）：

```
测试点：
  - Follower 选举超时 → Candidate
  - Candidate 获得多数票 → Leader
  - Leader 收到更高 term → Follower
  - Candidate 收到更高 term → Follower
  - 多个 node，只有票数最多的成为 Leader
```

### 8.3 Raft 集成测试

**raft_integration_test.cc** — 端到端集成：

```
测试场景：
  1. 单节点 SET/DEL 命令应用
  2. 多节点日志复制（AppendEntries）
  3. 多数派确认后 commit
  4. Leader 选举（多个 node 中最新的 log 的当选）
  5. Write 命令提交 → commit → apply → 状态机验证
```

**raft_multi_group_test.cc** — 多 Raft 组：

```
测试场景：
  1. 两个独立 group 并行选举各自的 leader
  2. 独立日志复制，互不干扰
  3. 每个 group 有自己的状态机
```

### 8.4 日志存储测试

**file_log_storage_test.cc**：

```
测试场景：
  - 基本 append/get 操作
  - 跨 segment 读取
  - 崩溃恢复：reopen 后读取之前写入的数据
  - TruncateFrom 截断
  - CompactUpTo 日志压缩
```

**segment_log_storage_test.cc**：

```
测试场景：
  - 基本 append/get
  - Segment 旋转（文件超过阈值自动切新文件）
  - 损坏尾部条目恢复
  - Segment 文件校验
  - 跨 segment 索引跟踪
```

### 8.5 Snapshot 测试

**snapshot_writer_test.cc + snapshot_loader_test.cc**：

```
测试场景：
  - 空 snapshot 写入/加载
  - 单个记录 → 写入 → 加载 → 验证
  - 多个记录 round-trip
  - 大记录 (>4KB) 处理
  - 加载不存在的文件 → 报错
  - 加载损坏文件 → 报错
```

**snapshot_sender_test.cc + snapshot_receiver_test.cc**：

```
测试场景：
  - 单 chunk 快照传输（<64KB）
  - 多 chunk 快照传输（>64KB，切割发送）
  - 临时文件清理
  - 最终文件重命名
```

**raft_apply_recovery_test.cc** — 应用恢复：

```
测试场景：
  1. 写入若干条命令 → snapshot 保存
  2. 再写入若干条 → 重启
  3. 从 snapshot 恢复 + 回放 snapshot 之后的日志
  4. 验证最终状态正确
```

### 8.6 RESP 协议测试

**redis_parser_test.cc**：

```
测试场景：
  - Inline 命令: "PING\r\n" → 正确解析
  - MultiBulk: "*3\r\n$3\r\nSET\r\n$1\r\na\r\n$1\r\n1\r\n" → 正确解析
  - 增量解析: INPUT_PENDING 返回（半包测试）
  - 边界情况: 空数组 "*0\r\n"、null bulk "$-1\r\n"
  - Pipeline: 两条命令连续发送，正确分拆解析
```

### 8.7 状态机测试

**kv_state_machine_test.cc**：

```
测试场景：
  - SET + GET round-trip
  - DEL: 删除存在/不存在的 key
  - EXPIRE: 设置过期，等待过期后 GET 返回 nil
  - Snapshot save/load:
    - 写入 100 个 key → SaveSnapshot
    - 清空状态机 → LoadSnapshot
    - 验证所有 key 恢复正确
```

**state_serializer_test.cc**：

```
测试场景：
  - Export: 从 DbSlice 导出 100 个 key/value
  - Import: 导入到新的 DbSlice
  - 验证过期时间戳的保存和恢复
```

### 8.8 故障测试场景

| 场景 | 测试文件 | 验证内容 |
|------|------|------|
| Leader Crash | `raft_role_test` | Follower 选举超时 → 选出新 Leader |
| Follower Crash | `raft_integration_test` | Leader 继续工作，多数派不受影响 |
| Network Partition | `raft_role_test` | 少数派无法选举出新 Leader |
| Log 不一致 | `raft_role_test` | AppendEntries 中 prevLogIndex 冲突 → truncate |
| Snapshot 恢复 | `raft_apply_recovery_test` | snapshot + log 回放 = 完整状态 |
| WAL 恢复 | `wal_writer_test` | 重启后读取之前的 WAL 记录 |
| Segment 损坏 | `segment_log_storage_test` | 损坏尾条目被跳过，破坏前数据可读 |
| 过期 Key | `kv_state_machine_test` | expire 后 GET 返回 nil |

### 8.9 性能基准测试

**测试环境**：

```
- 硬件: 虚拟机 (2 CPU 核心)
- 序建: midi-redis --port=6380 --conn_threads=2
- 客户端: Python 3 单线程 TCP socket, RESP 协议编码/解码
- Key 空间: 1000 个随机 key (GET), 随机 key (SET)
- 每项测试: 30,000 请求, 预热 1,000 请求
```

**实测数据**：

```
=================================================================
  Test                                QPS       P50       P99
  -------------------------------------------------------
  PING                             40,007     25.2us    46.3us
  SET 10B                          38,955     25.3us    53.7us
  GET 10B                          18,574     49.0us   167.3us
  DEL                              39,469     25.2us    47.7us
  INCR                             40,262     25.1us    43.4us
  EXPIRE                           39,278     25.4us    46.2us
  GET Pipeline x16                 173,418         -         -
  GET Pipeline x64                 631,437         -         -
=================================================================
```

**面试可用数据**：

| 指标 | 数据 | 说明 |
|------|------|------|
| SET QPS | **~39K** | 单连接串行，Raft FastCommitPath |
| SET P50 延迟 | **25us** | Append+Commit+Apply 全路径 |
| SET P99 延迟 | **54us** | 极端情况仍 < 100us |
| GET QPS | **~19K** | 单连接串行（受 Python 客户端影响） |
| GET P50 延迟 | **49us** | DbSlice hash 查找 |
| Pipeline x64 GET | **631K QPS** | 服务端真实吞吐上限 |
| PING QPS | **~40K** | 纯 RESP 解析+编码路径 |

**GET 为什么比 SET 慢？**

> GET 走的是异步调度路径：`Service::Get` 通过 `engine_.Schedule(key, cb)` 把回调投递到分片线程的 `FiberQueue`，然后直接返回。响应要等分片线程 fiber 取出任务 → 执行 `DbSlice::Find` → 调用 `SendGetReply` 才写入 socket。比 SET 多了一次跨 fiber 调度。SET 全程在同一个 fiber 调用栈内同步完成。
>
> Pipeline 掩盖了这次调度的开销——Pipeline x64 下调度延迟被 64 条命令平摊，达到 631K QPS，这才是服务端的真实处理能力。

**面试回答模板**：

> 我们在 2 核虚拟机上跑了一次单连接 TCP 基准测试。SET 达到约 4 万 QPS，P50 延迟 25 微秒，P99 控制在 54 微秒以内——这包含了 RESP 解析、Raft FastCommitPath（Append+Commit+Apply）、DbSlice 写入、RESP 编码的全路径。GET 因为是异步调度到分片线程，单连接约 2 万 QPS。Pipeline 64 压到 63 万 QPS，证明服务端 CPU 处理能力远未饱和，瓶颈在 Python 客户端串行 round-trip。另外我们有 32 个测试文件，覆盖了 Raft 协议的完整测试：节点角色转换、选举超时、日志复制、冲突解决、快照创建传输和应用、WAL 持久化和恢复、分段日志管理等。

---

## 九、面试重点

### 9.1 腾讯/字节/美团/阿里面试官最可能问的问题

**必考题（90% 概率）**：

1. **你的分片是怎么实现的？为什么能无锁？**
   → 考察对共享状态和多线程的理解

2. **Fiber 和线程的区别？为什么快？**
   → 考察对操作系统和并发模型的理解深度

3. **Raft 读写路径有什么不同？**
   → 考察对分布式一致性协议的理解

4. **io_uring 相比 epoll 有什么优势？**
   → 考察对 Linux 内核 I/O 的理解

5. **如果 Leader 挂了怎么办？**
   → 考察 Raft 选举机制

6. **为什么用 absl::flat_hash_map？**
   → 考察对数据结构原理的理解

7. **TCP 粘包怎么处理？**
   → 考察对网络编程的理解

**高频题（70% 概率）**：

8. **你的项目相比 Redis 有什么优势？为什么不用 Redis 源码？**
   → 考察项目动机

9. **Pipeline 怎么支持？怎么保证响应顺序？**
   → 考察并发编程

10. **Snapshot 和 AOF 的关系？恢复流程？**
    → 考察持久化机制

11. **跨分片操作怎么做？比如 MGET 多个 key 在不同分片？**
    → 考察分布式系统设计

12. **什么是脑裂？Raft 怎么防止脑裂？**
    → 考察分布式一致性

**深度题（30% 概率）**：

13. **io_uring 的 SQ poll 模式怎么工作？适合什么场景？**
    → 考察内核知识深度

14. **Swiss Table 的 SIMD 加速原理？**
    → 考察底层优化能力

15. **Raft 的 Joint Consensus 是什么？怎么实现的？**
    → 考察 Raft 延伸阅读

16. **FiberQueue 是 lock-free 的吗？怎么实现的？**
    → 考察并发数据结构

### 9.2 为什么会问这些问题

| 问题 | 面试官在考察什么 |
|------|------|
| 分片无锁 | 多线程编程能力，理解共享 vs 不共享 |
| Fiber vs Thread | 对操作系统调度、上下文切换的底层理解 |
| Raft 读写分离 | 分布式系统设计能力，一致性 vs 性能 tradeoff |
| io_uring | Linux 内核 I/O 深度，不是只会用 epoll |
| Leader 崩溃 | 故障恢复设计，高可用思维 |
| flat_hash_map | 数据结构原理，cache locality 意识 |
| TCP 粘包 | 网络编程基本功，字节流处理能力 |

### 9.3 优秀回答的特征

1. **不脱离源码**：能说出具体文件、行号、函数名
2. **有数据**：能说出为什么（64KB vs 8MB、50ns vs 1us、O(1) vs O(n)）
3. **有 tradeoff 意识**：不会只讲优点不讲缺点
4. **有架构思维**：能串起整个流程，不孤立地讲某个模块
5. **有技术深度**：能讲到 Swiss Table SIMD、io_uring SQ poll、Epoch-based 定时器等细节

### 9.4 哪些回答容易暴露项目不是自己做的

1. **只会讲概念，说不出源码细节**：说 "我用了一个状态机解析 RESP" 但说不出有几个状态、状态转换关系
2. **技术选型没有理由**：说 "我用了 Fiber" 但不知道为什么不直接用线程
3. **不知道项目的缺点**：任何项目都有取舍，只会夸是不行的
4. **说不出碰到过的坑**：比如 "io_uring 在低版本内核上兼容性问题"、"Fiber 栈溢出调试困难"
5. **答不出交叉追问**：比如说了分片无锁，但被问 "跨分片 MGET 怎么办" 答不上来

### 9.6 面试题详细回答

#### 1. 分片是怎么实现的？为什么能无锁？

**分片实现**：`EngineShardSet` 维护 `vector<FiberQueue*>`，数量 = CPU 核数 - 1（或 = CPU 核数）。每个 `EngineShard` 包含一个 `DbSlice`（其 `MainTable` 是 `absl::flat_hash_map<string, PrimeValue>`），通过 `Shard(key) = XXH64(key) % shard_num` 确定 key 归属。

**无锁原因**：
- **无共享**：每个 shard 绑定到一个 **线程 + 一个 fiber**（`fiber_q_` 循环消费），跨 shard 不共享任何可变数据。
- **定点投递**：`Set("a", "1")` 时，先 `Shard("a")` 得到 `sid`，再通过 `shard_set_.Await(sid, cb)` 把回调投到目标线程的 `FiberQueue`，在目标线程上同步执行。
- `Await` 内部用 `Done` + `EventCount` 挂起当前 fiber（不阻塞 OS 线程），目标 fiber 执行完后唤醒。

所以不是"无锁算法"，而是 **无共享架构 + fiber 投递**——不需要锁，因为数据从不被两个执行流同时访问。

```cpp
// common_types.h:47-50
template <typename View>
inline ShardId Shard(const View& v, ShardId shard_num) {
  XXH64_hash_t hash = XXH64(v.data(), v.size(), 120577);
  return hash % shard_num;
}

// engine_shard_set.h:31-33
class EngineShard {
  DbSlice db_slice;                          // 只被本线程访问
  static thread_local EngineShard* shard_;   // thread_local 单例
};
```

#### 2. Fiber 和线程的区别？为什么快？

| | OS 线程 | Fiber |
|---|---|---|
| 调度者 | 内核 | 用户态调度器 |
| 切换代价 | 陷入内核 ~100ns-1μs | 用户态 ~10-30ns |
| 栈大小 | ~8MB（RLIMIT_STACK） | 默认 64KB（可配置） |
| 数量上限 | ~10³ | ~10⁵+ |
| 语义 | 抢占式（内核时间片） | 协作式（只在 `yield`/`sleep`/`recv` 等处切换） |

**为什么快**：Helio Fiber 基于 `boost::context::fiber_context`（`swapcontext` 级），切换只需**保存/恢复寄存器 + 栈指针**，不涉及系统调用。且**协作式调度**杜绝了不必要的抢占中断。

```cpp
// ThisFiber::Yield() 调用链：
ThisFiber::Yield()
  → FiberInterface::Yield()           // detail/fiber_interface_impl.h:22
    → scheduler_->Preempt(true)       // 将当前 fiber 放回 ready queue
      → AddReady(active_fiber)
      → PopNext() → fi->SwitchTo()    // boost::context 切换
```

#### 3. Raft 读写路径有什么不同？

**写路径**（`SET`/`DEL`/`EXPIRE`）：
```
Set("a","1")
  → RaftEngine::SubmitCommand()
    → CommandEncoder::Encode("SET a 1")
    → 如果 not leader → 返回 ERROR("READONLY")
    → 如果 单节点 → FastCommitPath(): 追加日志 → AdvanceCommitIndex → ApplyCommittedLogs
    → 如果 多节点 → 追加日志 → ReplicateLog():
        → 发送 AppendEntries 给所有 peers
        → 收集响应 → AdvanceCommitIndex (majority 匹配)
        → ApplyCommittedLogs → KvStateMachine::Set() → shard_set_.Await(sid, ...)
```

**读路径**（`GET`）：
```
Get("a")
  → RaftEngine::Get() 或 Service::Get()
    → 如果 ReadConsistency::kLinearizable → 先 ReadIndex()
        → 如果 lease 有效 → 直接读 commit_index_（fast path）
        → 否则 → 发 ReadIndex RPC 给所有 peers，等 quorum 确认领导权
        → WaitForApplied(read_index) → 等到 last_applied_ >= read_index
    → 如果是 kLocal（绕过 Raft）→ 直接走 KvStateMachine::Get()
        → shard_set_.Await(sid, ...) 在目标 shard 上 Find
```

读默认走 local 路径（`Service::Get` 用 `engine_.Schedule()` 绕过 Raft），只有上层要求**线性一致性读**时才会走 ReadIndex 协议。写必须经过 Raft log replication。

#### 4. io_uring 相比 epoll 有什么优势？

| | epoll | io_uring |
|---|---|---|
| 系统调用次数 | 每次 I/O 至少 2 次 syscall（`epoll_wait` + `read`/`write`） | **0~1 次**（SQ 提交 + CQ 收割可合并） |
| 数据拷贝 | read/write 涉及内核→用户 buffer 拷贝 | 支持 **registered buffers** + **buffer ring**（零拷贝） |
| 批处理 | 每次 epoll_wait 返回一批事件，但还要逐个 read/write | **一次 submit 提交一批 SQE**，内核批量执行 |
| 异步深度 | 只支持 network socket + pipe | 支持文件、socket、accept、statx、fallocate、timeout 等 30+ 操作 |

项目中启用了 `IORING_SETUP_SUBMIT_ALL`、`poll_first`（内核先 poll 再走异步）、`buf_ring`（io_uring 提供 buffer，由内核填充数据到预注册的 buffer ring，完全零拷贝 recv）。

代码里有 `UringSocket::EnableRecvMultishot()` 让内核自动持续 recv 数据到 buf ring，**无需反复提交 SQE**。但 **没有启用 SQPOLL**（注释说需要提前注册所有 fd，短期不值得）。查看 `uring_proactor.cc:182`。

#### 5. Leader 挂了怎么办？

`RaftNode` 实现：

1. **心跳超时检测**：每个 Follower 的 `ElectionTimer` 运行一个 fiber，随机间隔 150-300ms（`election_timer.cc:dist_{150,300}`）。
2. **`OnElectionTimeout()`**（`raft_node.cc:184`）：`BecomeCandidate()` → 自增 term → 给自己投票。
3. **`StartElection()`**（`raft_node.cc:230`）：向所有 peers 发 `VoteRequest`（包含自己的 term 和最后日志的 index/term），等待 **majority**（N/2+1）投票。
4. **`TryBecomeLeader()`**（`raft_node.cc:277`）：如果票数 ≥ majority → `BecomeLeader()`，启动 `HeartbeatLoop`（每 50ms 发心跳）。

**防重复投票**：每个 term 只能投一票（`storage_.voted_for`），`OnRequestVote` 检查候选人的日志至少和自己一样新（`request.last_log_term >= local_last_term && last_log_index >= local_last_index`）。

#### 6. 为什么用 `absl::flat_hash_map`？

`common_types.h:62`：
```cpp
using MainTable = absl::flat_hash_map<std::string, MainValue>;
```

选择理由：
- **Swiss Table 算法**（open addressing + SIMD 探测）：一次加载 16 个 slot，用 SIMD 指令并行比较，cache 友好。
- **内存紧凑**：所有 key/value 存储在连续数组（slots），没有链表节点分配开销，load factor 87.5%。
- 对比 `std::unordered_map`（链式桶，指针跳跃，cache miss 严重）和 `std::map`（红黑树，O(log n) + 指针链），`flat_hash_map` 在**查找性能 + 内存局部性**上优势显著。
- **你的代码不需要有序遍历**（Redis 没有 `KEYS` 要求），不需要 ordered map。

#### 7. TCP 粘包怎么处理？

`dragonfly_connection.cc::InputLoop` 中用 `base::IoBuf` 做**积累式缓冲区**：

```
循环:
  1. DoRead(peer, ...) → 从 socket 读数据到 io_buf 的 AppendBuffer
  2. ParseRedis(&io_buf) → 解析完整的 RESP 命令
     - 如果数据不够（NEED_MORE）→ 返回循环首部继续读
     - 如果解析出一批完整命令 → 逐个执行
```

解析过程（`ParseMultiBulk`）：状态机跟踪 `multibulk_len_`（要读的 arg 数）和 `bulk_len_`（当前 arg 的长度）。只消费**完整命令**的数据，不完整的留在 `io_buf` 中，下次循环继续。

TCP 是流协议，没有消息边界。RESP 协议自带长度字段（`*3\r\n$3\r\n...`），所以解析器按长度精确消费，不多读、不少读。`io_buf` 负责积累和保留未消费的字节。

#### 8. 项目相比 Redis 有什么优势？为什么不用 Redis 源码？

**优势**：
1. **共享线程池 → fiber 单线程串行化**：每个 shard 只有一个 fiber，无需锁。Redis 是单线程，无法利用多核；本项目每个核一个 shard，线性扩展。
2. **io_uring 原生支持**：Redis 用 epoll（每次 Read/Write 都要系统调用），本项目用 io_uring 批量化提交，零拷贝 recv via buffer ring。
3. **Raft 强一致性**：原生 Redis 主从复制是异步（可能丢数据），本项目实现 Raft 共识，线性一致性读 + 安全容错。
4. **双协议支持**：RESP + Memcached ASCII。

**为什么不用 Redis 源码**：Redis 是单线程 epoll 模型，改造为多线程 + io_uring + Raft 需要大规模重写。从 Dragonfly 项目 fork 起点更好——Helio fiber 架构天然适合做这些改动。

#### 9. Pipeline 怎么支持？怎么保证响应顺序？

Pipeline 实现在 `Connection::InputLoop` 中：

- `ParseRedis` 解析到一批完整命令后，逐个执行：`while (cc_->to_execute)` → 取第一个 → `DispatchCommand()` → 移到下一个。
- `batch_mode` 标志：如果下一个命令也已经解析完成，设 `batch_mode = true`，让 reply 积累起来发送（`cc_->SetBatchMode(batch_mode)`）。

**顺序保证**：**隐式保证**，因为所有命令在**同一个 fiber 里顺序执行、顺序回复**。没有并发 dispatch，没有 reorder。Pipeline 只需要"攒一批命令再解析"，而不是并发处理它们。

`DispatchCommand` 里直接调用 `cid->Invoke(args, cntx)`，同步调用完才返回，然后回复，然后处理下一条。响应顺序天然等于请求顺序。

#### 10. Snapshot 和 AOF 的关系？恢复流程？

**Snapshot**：全量快照，保存所有 key/value。二进制格式（`SnapshotWriter` + `SnapshotLoader`），每 shard 并行导出（`RunBriefInParallel` → `StateSerializer::Export`）后合并写入 `snapshot.bin`。

**AOF**：Append-Only File，追加每条写命令的 RESP 格式日志。`AofWriter` 累积写入 `appendonly.aof`。

**关系**：
- **Snapshot 是快照**（某一时刻的全量数据），**AOF 是操作日志**（所有写操作序列）。
- Raft 日志（`CommandLog` / `FileLogStorage`）记录了所有写操作，AOF 更多是备选持久化方案。

**恢复流程**（`Service::Init`）：
```
1. shard_set_.Init() → 初始化所有 shard
2. LoadSnapshot() → 检查 snapshot.bin 是否存在
   → 按 key 分发到各 shard → StateSerializer::Import
3. persistence_manager_->Open("appendonly.aof")
4. ReplayAof() → 读 appendonly.aof → 解析 RESP 命令 → 重新 DispatchCommand
```

#### 11. 跨分片操作怎么做（MGET 多个 key 在不同分片）？

当前代码中没有实现 MGET（`CommandRegistry` 里只有 `PING`/`SET`/`GET`/`DEL`/`EXPIRE` 等）。

如果要实现 `MGET key1 key2 key3`（每个 key 可能在不同 shard），做法是：

```
1. 分组: Shard(key1) = 0, Shard(key2) = 2, Shard(key3) = 0
     → shard0: [key1, key3], shard2: [key2]
2. 并行投递: shard_set_.Await(0, cb0), shard_set_.Await(2, cb2)
     → 各自的 EngineShard 上执行 db_slice.Find
3. 融合结果: 按原始 key 顺序组装回复数组
```

利用 `BlockingCounter` 或 `RunBriefInParallel` 模式并行化，但需要维护原始 key 顺序。`EngineShardSet::Await(sid, f)` 已经支持跨线程投递（`FiberQueue::Add` → `Done::Wait`），所以实现 MGET 只是反序列化 + 分组 + 并行 Await + 顺序组装。

#### 12. 什么是脑裂？Raft 怎么防止脑裂？

**脑裂**：集群中同时出现两个 Leader，各自认为自己是主，导致数据不一致。

**Raft 防止脑裂的机制**：
1. **唯一 Leader**：一个 term 中最多只有一个 candidate 能收集到 majority 投票（`TryBecomeLeader`：`votes_received >= total/2 + 1`）。
2. **Term 仲裁**：Leader 的 `AppendEntries`/`Heartbeat` 中包含自己的 term。Follower 发现 term 更新时，立刻 `BecomeFollower(new_term)`。Leader 收到更高 term 的响应时，自动退位（`ReadIndex` 中 `if (resp.term > current_term) BecomeFollower(resp.term)`）。
3. **Majority 提交**：日志条目只有在大多数节点确认复制后才能提交（`AdvanceCommitIndex` 取排序后的 majority 位置）。
4. **旧 Leader 隔离**：网络分区后，旧 Leader 的 term 低于新 Leader，无法提交新日志，即使它还在发心跳，也不会被接受。

代码体现：`OnHeartbeat`（`raft_node.cc:312`）检查 term；`OnAppendEntries`（`:589`）检查 term；`OnRequestVote`（`:190`）检查 term 和日志新旧。

#### 13. io_uring 的 SQ poll 模式怎么工作？适合什么场景？

**SQ poll 原理**：
- 创建 io_uring 时设 `IORING_SETUP_SQPOLL` 标志。
- 内核启动一个**内核线程**（io_uring 的 sqpoll thread），持续轮询 SQ 队列，用户写入 SQE 后无需 `io_uring_enter` 系统调用。
- 用户通过 `mmap` 共享 SQ/CQ，与内核线程通过共享内存通信。

**优势**：**完全零系统调用**提交 I/O（只需写共享内存）。对于高频小 I/O（如大量 1KB 以下的 recv/send），优势显著。

**不适合场景**：
- 连接数频繁变化（SQPOLL 要求所有 fd 提前注册到 io_uring，动态 Accept 需要额外注册）。
- 批量大 I/O 时，系统调用代价已分摊，SQPOLL 优势不突出。

**你的代码中注释掉了 SQPOLL**（`uring_proactor.cc:182`）：
```cpp
// it seems that SQPOLL requires registering each fd, including sockets fds.
// need to check if its worth pursuing. For sure not in short-term.
```
因为 maxredis 是动态 Accept 连接的，每个新连接 fd 都需要注册到 io_uring，SQPOLL 模式下这个注册成本更高，所以暂不启用。

#### 14. Swiss Table 的 SIMD 加速原理？

**`absl::flat_hash_map`**（Google's Swiss Table）：

1. **数据结构**：将 slots 分组成 **Group**（每组 16 个 slot），每个 slot 存储 key/value + 一个 **控制字节**（`ctrl_t`，1 字节表示 slot 状态：empty/deleted/full+hash_prefix）。
2. **SIMD 探测**：查找 `key` 时：
   - 计算 hash → 取前 7 bit 作为 `hash_prefix`。
   - 从 `ctrl` 数组对应 Group **一次加载 16 个控制字节**到 SIMD 寄存器（SSE2 `_mm_loadu_si128` / SSE4.2 `_mm_cmpistrm` / ARM NEON）。
   - SIMD 指令**并行比较**这 16 个字节中哪些与 `hash_prefix` 匹配。
   - 对匹配的 slot，逐 key 精确比较。
   - 如果没找到，用 `_mm_prefetch` + 探测序列找下一个 Group。

**为什么快**：
- 一次 SIMD 指令完成 16 路比较，micro-op 级并行。
- 控制字节和 key 在连续内存中，cache line 利用率高（对比 `unordered_map` 链式指针跳跃）。
- 探测序列保证 ~90% 负载率下平均 1-2 次探测找到。

#### 15. Raft 的 Joint Consensus 是什么？怎么实现的？

**Joint Consensus**（Raft 论文 §6）：成员变更时，新旧配置**共存一段时间**，确保变更期间不会脑裂。

**实现**（`raft_node.h` 头部的 STRICT REQUIREMENTS 明确要求两阶段）：

**阶段 1（Stable → Joint）**：
- `BeginConfigChange(target)`：保存 `joint_config_.old_config = cluster_config_`，`joint_config_.new_config = target`。
- 追加一条 `CONFIG_CHANGE` 日志到 Raft log。
- 当该日志被 apply 时，`config_state_` 变为 `kJoint`。

**Joint 状态的特殊行为**：
- **选举**（`StartElection`）：需要 **old majority AND new majority** 同时投票（`old_config_votes_ >= old_majority && new_config_votes_ >= new_majority`）。
- **提交**（`AdvanceCommitIndexJoint`）：计算 old 和 new 两个配置各自的 majority commit index，取**最小值**：`commit_index = min(old_commit, new_commit)`。
- **ReplicateLog**：`GetPeerIds()` 返回 old + new 的所有 voter 并集。

**阶段 2（Joint → Stable）**：
- Leader 再次调 `BeginConfigChange(target)`（target 必须等于 `new_config`）。
- 追加第二条 `CONFIG_CHANGE` 日志。
- 当第二条日志 apply 时：`cluster_config_ = new_config`，`config_state_ = kStable`，退出 joint 状态。

核心目的：**变更过程中，不可能同时选出两个 Leader**（因为需要两个独立的 majority）。

```cpp
// raft_node.cpp:801-834
void RaftNode::AdvanceCommitIndexJoint() {
  LogIndex old_commit = calc_config_commit(joint_config_.old_config);
  LogIndex new_commit = calc_config_commit(joint_config_.new_config);
  LogIndex candidate = std::min(old_commit, new_commit);  // 取最小值！
  if (candidate > commit_index_)
    commit_index_ = candidate;
}
```

#### 16. FiberQueue 是 lock-free 的吗？怎么实现的？

**是**。`FiberQueue` 内部使用 `base::mpmc_bounded_queue`（Dmitry Vyukov 的著名设计），是一个 **lock-free MPMC bounded queue**（无锁多生产者多消费者有界队列）。

**算法核心**（`mpmc_bounded_queue.h`）：
- **有界循环数组**：大小必须是 2 的幂，每个 cell 有 `std::atomic<size_t> sequence` + `char storage[sizeof(T)]`。
- **sequence 的双重角色**：
  - 初始值：`sequence[i] = i`（free slot）。
  - 生产者：CAS 拿 `enqueue_pos_`，检查 `sequence == pos`（说明 slot 可用）→ 写入数据 → `sequence.store(pos+1)` 释放给消费者。
  - 消费者：CAS 拿 `dequeue_pos_`，检查 `sequence == pos+1`（说明数据就绪）→ 读取数据 → `sequence.store(pos+buffer_mask_+1)` 归还给生产者。
- **无锁**：只用 `std::atomic` + CAS（`compare_exchange_weak`），无 mutex。
- **Cache-line padding**：`enqueue_pos_`、`dequeue_pos_` 各占一个 cacheline（`pad0_/pad1_/pad2_/pad3_`），避免 false sharing。

**FiberQueue 的阻塞机制**：当队列满时，生产者 fiber 通过 `push_ec_`（EventCount）挂起；当队列空时，消费者 fiber 通过 `pull_ec_` 挂起。EventCount 只挂起 fiber（不阻塞 OS 线程），唤醒时立刻尝试入队/出队。

所以 `FiberQueue` 是：**lock-free 的 MPMC queue + fiber-friendly 阻塞协调**。

---

### 9.5 容易被继续深挖的地方

| 第一个答案 | 可能的深挖追问 |
|------|------|
| "分片无锁访问" | → "MGET keys 在不同分片怎么处理？" → "分布式事务？" |
| "Fiber 切换快" | → "jump_fcontext 保存了哪些寄存器？" → "栈大小怎么确定的？" |
| "io_uring 零拷贝" | → "Fixed buffers 怎么注册的？" → "SQ poll 什么时候用？" |
| "Raft 读写分离" | → "读了 stale 数据怎么办？" → "ReadIndex/LeaseRead 怎么实现的？" |
| "hash 表 O(1)" | → "hash 碰撞怎么办？" → "rehash 是渐进式的还是全量的？" |
| "Snapshot 用于恢复" | → "做 snapshot 的时候能写吗？" → "SnapshotBarrier 是怎么工作的？" |
| "FastCommitPath" | → "什么时候不走 FastCommit？" → "多节点时 latency 增加多少？" |

---

## 十、项目介绍

### ① 一分钟项目介绍（电梯 pitch）

> 我基于 C++ 17 从零实现了一个兼容 Redis RESP 协议的分布式 KV 存储。核心架构是三件事：① 多线程 key-hash 分片，每个分片一个 lock-free 的 Swiss Table，数据按 key 路由到不同 CPU 核心并行处理；② 用 Fiber 协程替代线程处理连接，64KB 栈，io_uring 批量 I/O，系统调用减少 1-2 个数量级；③ 自研了完整的 Raft 共识协议，支持选举、日志复制、快照、成员变更等，32 个测试覆盖了正常路径和故障恢复。实测数据：SET 约 4 万 QPS/P50=25us/P99=54us，Pipeline 64 达到 63 万 QPS。

### ② 三分钟项目介绍

> 我做的这个项目是一个兼容 Redis RESP 协议的高性能分布式 KV 存储。它的核心存储架构和 Redis 完全不同——不是单线程模型，而是多线程分片架构。
>
> 架构上最核心的是三层：
>
> **存储层**——数据按 key 做 XXH64 hash 然后分片到多个 CPU 核心，每个分片绑定一个线程，内部用 `absl::flat_hash_map`（Swiss Table）存储数据。同一线程访问本分片数据完全无锁，不同分片在不同线程上并行处理。跨线程通过 `FiberQueue` 消息传递——`Await(sid, task)` 把任务投递到目标线程，执行完后唤醒调用方 fiber。
>
> **网络和调度层**——每个 TCP 连接一个 Fiber 协程处理，默认栈只有 64KB。I/O 底层用 io_uring 替代 epoll，批量提交 SQE、批量收割 CQE，系统调用数降低一个数量级。`UringProactor` 的 `MainLoop` 是核心事件循环，SQ 和 CQ 通过共享内存环形缓冲区和内核交互。利用 Buffer Ring 预注册接收缓冲区，recv 数据零拷贝。
>
> **一致性层**——自研了完整的 Raft 协议。`RaftNode` 实现 Follower/Candidate/Leader 三态转换，单个 `SetRole()` 统一入口管理 timer 和 heartbeat fiber 生命周期。读写路径做了分离：GET 直接 `Schedule` 到分片线程查 hash 表，不经过 Raft；SET/DEL 走 `SubmitCommand` → `FastCommitPath`（单节点 Append+Commit+Apply 三步）或 `ReplicateLog`（多节点等多数派）。`ElectionTimer` 用随机 150-300ms 的 Fiber `SleepFor` 实现选举超时，`HeartbeatLoop` 是 Leader 上 50ms 间隔的心跳 Fiber。
>
> 测试方面有 32 个测试文件覆盖正常路径和故障路径——选举、日志复制、快照创建/传输/恢复、WAL 恢复、分段日志管理、节点崩溃、网络分区都能验证。

### ③ 五分钟项目介绍

（在三分钟基础上增加技术细节和设计权衡）

**补充技术细节**：

> RESP 协议解析部分有两个解析器：一个完整状态机 `RedisParser`（7 个状态，支持嵌套 Array），一个简化版 `ParseMultiBulk`（直接按 `*N`→循环读 `$len`→读数据）。实际生产用简化版，因为 99% 的 Redis 命令都是这种简单 bulk 格式。半包处理靠在 `Connection` 对象中保留 `multibulk_len_` 和 `bulk_len_` 状态变量，返回 `NEED_MORE` 继续读。
>
> CommandRegistry 用了 `absl::flat_hash_map<string_view, CommandId>` 做命令分发，每个 `CommandId` 携带命令名、标记位（WRITE/READONLY/FAST 等）、参数个数、key 位置等元数据。加新命令只需 `registry_ << CI{...}.HFUNC(Handler)` 一行。
>
> RaftEngine 的 `SubmitCommand` 有三条路径：`CommandEncoder::Encode()` 返回 `nullopt`（读命令/PING）→ 直接 `kv_.Apply()`；单节点 (`PeerCount==0`) → `FastCommitPath`；多节点 → `Append` + `ReplicateLog` → 等多数派。`KvStateMachine` 的 `ApplyLogEntry` 从 "SET key val" 字符串解析出命令类型和参数，通过 `Shard(key)` 计算分片，`Await(sid)` 投递到目标分片。
>
> Snapshot 方面做了增量触发（时间阈值 or 命令数阈值），`SnapshotBarrier` 在 snapshot 期间保护读写一致性。AOF 通过 `PersistenceManager` 追加写命令到 `appendonly.aof`，启动时 `ReplayAof` 回放。还有一套独立的快照格式（二进制 key_len+value_len+expire_at）。

**设计权衡**：

> 不做的事情和做的事情一样重要：① 不做跨分片事务（MGET 跨分片时无法保证原子性），简化设计；② 不做复杂数据类型（只支持 String），因为重点是分布式一致性引擎而不是数据结构；③ RPC 层目前是进程内 `LocalTransport`，网络传输层留了接口但未接入——因为当前重点是验证 Raft 协议本身的正确性，网络层可以后续加。

### ④ 每个模块怎么介绍

| 模块 | 30 秒版本 | 核心要点 |
|------|------|------|
| RESP 协议 | 自研状态机解析器，支持半包和 Pipeline | 7 个状态，零拷贝 Span，两个解析器 |
| 命令路由 | flat_hash_map O(1) 查找，一行加命令 | arity 校验，CommandId 元数据 |
| 存储引擎 | Swiss Table 分片存储，无锁访问 | XXH64 hash，DbSlice，懒惰过期 |
| Fiber | 用户态协程，64KB 栈，~50ns 切换 | EventCount 同步，FiberQueue |
| io_uring | 批量 I/O，SQ/CQ 共享内存 | Buffer Ring，Multishot Recv |
| Raft | 三态转换，日志复制，读线性一致 | FastCommitPath，选举随机超时 |
| Snapshot | 增量触发，分片并行导出 | SnapshotBarrier，二进制格式 |
| 测试 | 32 个测试文件，故障全覆盖 | Raft 核心+集成+快照+AOF |

### ⑤ 每一部分如何自然过渡

```
"除了存储层的分片设计，我们还针对 I/O 路径做了优化..."
  → 过渡到 Fiber + io_uring

"数据落地只是单机的问题，如果要支持多节点部署..."
  → 过渡到 Raft 共识

"协议层和存储层之间还有一个关键问题——怎么查表找到命令..."
  → 过渡到 CommandRegistry

"一致性协议理论上很好，但如何验证它确实正确..."
  → 过渡到测试体系
```

---

## 十一、最终总结

### ① 项目知识树

```
maxredis (midi-redis)
│
├── 协议层 (protocol/)
│   ├── RESP 解析 (RedisParser + ParseMultiBulk)
│   ├── RESP 编码 (ReplyBuilder / RespSerializer)
│   ├── Memcache 协议 (可选)
│   └── 连接上下文 (ConnectionContext / ParsedCommand)
│
├── 服务层 (service/)
│   ├── TCP 监听 (DragonflyListener)
│   ├── 连接管理 (Connection / InputLoop)
│   ├── 命令注册 (CommandRegistry + CommandId)
│   ├── 命令处理 (MainService: SET/GET/DEL/EXPIRE/PING/DEBUG/INFO)
│   ├── 快照调度 (SnapshotFiber)
│   └── 启动入口 (dfly_main.cc)
│
├── 存储层 (storage/)
│   ├── 分片引擎 (EngineShardSet + EngineShard)
│   ├── KV 存储 (DbSlice)
│   ├── 数据类型 (PrimeValue, MainTable, OpResult)
│   └── 哈希路由 (XXH64 Shard)
│
├── Raft 层 (raft/)
│   ├── 核心 (RaftNode: 角色转换/选举/日志复制)
│   ├── RPC (Vote/AppendEntries/Heartbeat/Snapshot/ReadIndex)
│   ├── 日志 (CommandLog / SegmentLogStorage / WAL)
│   ├── 快照 (SnapshotWriter/Loader/Sender/Receiver/Manager)
│   ├── 定时器 (ElectionTimer: 150-300ms 随机)
│   ├── 编码 (CommandEncoder + ReplicatedCommand)
│   ├── Peer 管理 (PeerManager)
│   └── 传输 (Transport / LocalTransport)
│
├── 状态机 (state_machine/)
│   ├── 接口 (IStateMachine)
│   └── 实现 (KvStateMachine: Apply/ApplyLogEntry/Snapshot)
│
├── 持久化 (persistence/)
│   ├── AOF (AofWriter + PersistenceManager)
│   └── 快照 (SnapshotManager: encode/decode)
│
├── 网络/调度 (helio/)
│   ├── Fiber (Fiber + Scheduler + EventCount)
│   ├── io_uring (UringProactor + UringSocket)
│   ├── epoll (EpollProactor)
│   ├── TLS (TlsSocket)
│   └── 连接池 (ProactorPool + AcceptServer)
│
└── 测试 (32 files)
    ├── Raft 核心 (5)
    ├── Raft RPC (5)
    ├── Raft 日志 (4)
    ├── Raft Snapshot (6)
    ├── Raft 集成 (3)
    └── 状态机/持久化/协议 (9)
```

### ② 核心技术点

1. **Sharded Storage**: Key hash → Thread binding, Lock-free per shard
2. **Fiber 协程**: User-space cooperative scheduling, 64KB stack, ~50ns switch
3. **io_uring**: Batch SQ/CQ, Buffer Ring, Multishot Recv, Fixed FDs
4. **Swiss Table**: SIMD probing, Open addressing, Cache-friendly
5. **Raft Consensus**: Role transitions, Log replication, Snapshot, Joint Consensus
6. **RESP Protocol**: State machine parser, Zero-copy, Half-packet handling
7. **Read/Write Split**: Reads bypass Raft, Writes go through log replication
8. **FastCommitPath**: Single-node optimization, no network overhead

### ③ 必须掌握的源码（按优先级）

| 优先级 | 文件 | 必须掌握的内容 |
|------|------|------|
| P0 | `storage/db_slice.cc` | Find/AddOrFind/Del 实现，过期处理 |
| P0 | `storage/engine_shard_set.cc` | Init/Await/RunBriefInParallel |
| P0 | `service/main_service.cc` | DispatchCommand, Set/Get 分流 |
| P0 | `raft/raft_engine.cc` | SubmitCommand, FastCommitPath |
| P0 | `raft/raft_node.cc` | SetRole, BecomeFollower/Candidate/Leader |
| P1 | `service/dragonfly_connection.cc` | InputLoop, ParseMultiBulk, DoRead |
| P1 | `state_machine/kv_state_machine.cc` | Apply, ApplyLogEntry, Set/Del |
| P1 | `protocol/reply_builder.cc` | SendBulkString, SendSimpleString, SendError |
| P1 | `raft/election_timer.cc` | Run, Reset, epoch 机制 |
| P2 | `protocol/redis_parser.cc` | 状态机 7 个状态，ConsumeBulk |
| P2 | `service/command_registry.cc` | CommandId, Find, operator<< |
| P2 | `raft/command_encoder.cc` | Encode, IsWriteCommand |
| P3 | `persistence/persistence_manager.cc` | RecordCommand, Load, AofWriter |

### ④ 高频面试题（30+）

**存储与并发**：
1. 你的分片是怎么实现的？加锁吗？
2. Key 如何路由到 Shard？为什么用 XXH64？
3. 跨分片操作怎么做？（如 MGET 不同 shard 的 key）
4. Await(sid, lambda) 内部是怎么实现的？
5. FiberQueue 是 lock-free 的吗？
6. 一个 Shard 上的操作顺序如何保证？

**Fiber 与 io_uring**：
7. Fiber 和线程的区别？为什么切换快？
8. boost.context 的 jump_fcontext 做了什么？
9. 为什么 Fiber 的默认栈是 64KB？
10. io_uring 相比 epoll 的优势是什么？
11. SQ/CQ 是怎样的？为什么能减少 syscall？
12. Buffer Ring 和 Multishot Recv 怎么工作的？

**数据结构**：
13. absl::flat_hash_map 比 std::unordered_map 好在哪里？
14. Swiss Table 的 SIMD 探测原理？
15. hash 碰撞了怎么办？rehash 是逐步的吗？
16. 为什么用 string 而不是更高效的内存管理？

**Raft 一致性**：
17. Raft 三态转换的过程？
18. GET 和 SET 的路径有什么不同？为什么？
19. FastCommitPath 什么时候走？什么时候不走？
20. Leader 挂了怎么办？选举超时是多少？
21. 如果网络分区，有多少节点能正常工作？
22. 线性一致读怎么实现？
23. 日志冲突怎么解决（AppendEntries 的 prevLogIndex 不匹配）？
24. Joint Consensus 是什么？什么时候用？

**持久化与恢复**：
25. Snapshot 和 AOF 有什么区别？恢复顺序是什么？
26. 做 Snapshot 的时候能写吗？（SnapshotBarrier）
27. SegmentLogStorage 的分段策略是什么？
28. WAL 的 CRC 校验在哪里？

**协议与网络**：
29. TCP 粘包拆包怎么处理？
30. RESP 解析器有几个状态？怎么做零拷贝？
31. Pipeline 响应顺序怎么保证？
32. 如果客户端发了 1GB 的 Bulk String 怎么办？

**设计与权衡**：
33. 为什么不用 Redis 源码？你的优势是什么？
34. 项目的最大技术挑战是什么？
35. 如果再给你一个月，你会做什么改进？

### ⑤ 每题标准回答（精选）

因篇幅限制，此处列出最关键的几个：

**Q17: Raft 三态转换？**

> `SetRole(new_role)` 是唯一的角色切换入口。Follower 状态下若 `ElectionTimer` 超时（150-300ms 随机），调用 `OnElectionTimeout` → `BecomeCandidate`：term++，投自己一票，向所有 peer 发 `VoteRequest`。收到多数票 → `BecomeLeader`：停掉 `ElectionTimer`，启动 `HeartbeatLoop` fiber 每 50ms 发心跳。Leader/Candidate 收到更高 term 的消息 → `BecomeFollower(term)`：更新 term，清空 voted_for，重置选举定时器。这套机制在 `raft_node.cc:140-166`。

**Q27: Snapshot 的时候能写吗？**

> `SnapshotBarrier` 提供了一个读锁。做 snapshot 时 `barrier_->BeginRead()` 获取读锁，普通写操作也获取读锁，snapshot 写入操作需要写锁。所以做 snapshot 期间读写可以并发，但触发 compaction 时才需要写锁阻塞。实现在 `raft/snapshot_barrier.h`。

**Q30: RESP 解析器状态机？**

> 7 个状态：`INIT_S` → 根据首字符分叉到 `ARRAY_LEN_S`（`*`）、`PARSE_ARG_S`（`$`）、`INLINE_S`（其他）。`ARRAY_LEN_S` 解析 `*N\r\n` 中的 N，然后进入 `PARSE_ARG_S` 循环解析每个 arg。`PARSE_ARG_S` 读到 `$N\r\n` 后进入 `BULK_STR_S` 读 N 字节数据。读完一个 arg 进入 `FINISH_ARG_S` 检查是否还有剩余，有的话回到 `PARSE_ARG_S`，没有则到 `CMD_COMPLETE_S`。解析栈 `parse_stack_` 支持嵌套 Array。零拷贝：`bulk_str = str.subspan(0, bulk_len_)` 直接引用 IoBuf 数据。

**Q33: 为什么不用 Redis 源码？优势？**

> Redis 的核心问题是单线程事件循环，受限于单核。我的项目用多线程 key-hash 分片实现水平扩展。此外集成 Raft 提供分布式一致性，配备了完整的 Raft 测试体系。但不是替代 Redis——我的项目不支持复杂数据类型，不是 Redis Drop-in Replace，重点在分布式存储引擎的技术验证。

### ⑥ 最容易被追问的地方

1. **MGET 跨分片** → 分布式事务或 scatter-gather 模式
2. **Raft 读写分离后读到的不是最新数据** → ReadIndex / Lease Read
3. **Snapshot + 写入并发** → SnapshotBarrier 机制
4. **Multi-Raft 的 group 如何管理** → RaftGroupManager
5. **LocalTransport vs 真实网络** → 序列化、网络分区、超时重试
6. **SegmentLog 的 GC** → CompactUpTo 的实现
7. **io_uring 的 SQ poll vs 中断模式** → 适用场景分析
8. **Fiber 栈溢出** → Guard page / 检测机制
9. **AOF 的 fsync 策略** → always/everysec/no
10. **为什么选 Raft 而不是 Paxos** → 可理解性、工业实现更多

### ⑦ 面试前 Checklist

**必须能脱稿说清楚**：
- [ ] GET 一条命令的完整代码路径（文件名+行号）
- [ ] SET 一条命令的完整代码路径（含 Raft 路径）
- [ ] Shard(key) 的计算公式
- [ ] Await(sid, lambda) 的跨线程机制
- [ ] Fiber::SleepFor 为什么不阻塞线程
- [ ] io_uring 的 SQ/CQ 环形队列结构
- [ ] Raft 三态转换代码（SetRole 函数）
- [ ] ElectionTimer 的 epoch 防竞态机制
- [ ] Swiss Table 的 SIMD 加速原理
- [ ] FastCommitPath 的三个步骤
- [ ] 至少 5 个测试文件名和测试内容
- [ ] 项目的最大技术挑战和解决方案

**必须能画出来的图**：
- [ ] 完整请求链路图（Client → Listener → Parser → Registry → Service → RaftEngine → KvStateMachine → DbSlice → ReplyBuilder）
- [ ] 分片架构图（N 个线程 / N 个 Shard / key hash 路由）
- [ ] Fiber 调度示意图（Proactor → Scheduler → FiberQueue → EventCount）
- [ ] Raft 三态转换图（Follower ↔ Candidate ↔ Leader）
- [ ] io_uring SQ/CQ 示意图

**必须能回答的 "为什么"**：
- [ ] 为什么分片不加锁？
- [ ] 为什么用 Fiber 不用线程？
- [ ] 为什么用 io_uring 不用 epoll？
- [ ] 为什么用 flat_hash_map 不用 unordered_map？
- [ ] 为什么读写路径分离？
- [ ] 为什么用 XXH64 不用 SHA256？
- [ ] 为什么用 Raft 不用 Paxos？
- [ ] 为什么自己实现 RESP 解析而不是用 hiredis？

---

## 附录：关键源码速查表

| 功能 | 文件 | 关键行 |
|------|------|------|
| 启动入口 | `service/dfly_main.cc` | 92-145 |
| TCP 监听 | `service/dragonfly_listener.cc` | 96-98 |
| Connection 创建 | `service/dragonfly_connection.cc` | 67-78 |
| I/O 循环 | `service/dragonfly_connection.cc` | 175-262 |
| RESP 解析 | `service/dragonfly_connection.cc` | 454-536 |
| 半包处理 | `service/dragonfly_connection.cc` | 493-499 |
| 命令注册 | `service/main_service.cc` | 517-528 |
| 命令分发 | `service/main_service.cc` | 193-218 |
| GET 处理 | `service/main_service.cc` | 311-327 |
| SET 处理 | `service/main_service.cc` | 283-308 |
| Raft 提交 | `raft/raft_engine.cc` | 21-43 |
| FastCommit | `raft/raft_engine.cc` | 45-53 |
| 状态机 Apply | `state_machine/kv_state_machine.cc` | 36-47 |
| 状态机 Set | `state_machine/kv_state_machine.cc` | 49-60 |
| 状态机 Get | `state_machine/kv_state_machine.cc` | 88-100 |
| 分片路由 | `storage/common_types.h` | 47-50 |
| Shard 无锁 | `storage/engine_shard_set.cc` | 28-33 |
| Await 实现 | `storage/engine_shard_set.h` | 55-58 |
| DbSlice::Find | `storage/db_slice.cc` | 42-64 |
| DbSlice::AddOrFind | `storage/db_slice.cc` | 78-104 |
| 过期处理 | `storage/db_slice.cc` | 58-62 |
| RESP 回复 | `protocol/reply_builder.cc` | 95-114 |
| 错误回复 | `protocol/reply_builder.cc` | 154-164 |
| Heartbeat | `raft/raft_node.cc` | 373-409 |
| ElectionTimer | `raft/election_timer.cc` | 全文 |
| SnapshotFiber | `service/snapshot_fiber.cc` | 全文 |
| io_uring 初始化 | `service/dfly_main.cc` | 116-119 |
| BufRing 注册 | `service/dfly_main.cc` | 41-68 |
| Multishot Recv | `service/dragonfly_connection.cc` | 179-190 |
